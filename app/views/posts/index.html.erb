<h4>一覧</h4>
<!-- 投稿一覧 -->
<% @posts.each do |post| %>
	<p>image</p>
	<%= link_to post_path(post.id) do %>
		<%= attachment_image_tag post, :image, :fill, 100, 100, format: 'jpeg', fallback: "no-image-mini.jpeg" %>
	<% end %>
	<p>title</p>
	<%= post.title %>
	<p>text</p>
	<%= post.text %>

<% end %>