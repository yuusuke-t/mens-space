<h4>編集</h4>

<%= form_with(model: @post, local: true) do |f| %>
	<h4>image</h4>
	<%= f.attachment_field :image, placeholder: "プロフィール画像" %>
	<h4>title</h4>
	<%= f.text_field :title %>
	<h4>text</h4>
	<%= f.text_area :text %>

	<%= f.submit "変更を保存" %>

<% end %>